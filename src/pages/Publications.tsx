import MarkdownPage from "@/components/MarkdownPage";
import { publicationsContent } from "@/content/publications";

const Publications = () => <MarkdownPage content={publicationsContent} />;

export default Publications;
